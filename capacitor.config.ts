import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danmonceau.fr',
  appName: 'portofolio-crypto',
  webDir: 'dist/PortofolioCrypto',
  bundledWebRuntime: false
};

export default config;
